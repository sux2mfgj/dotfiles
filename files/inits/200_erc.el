;; -*- tab-width: 4; coding: utf-8; lexical-binding: t -*-

(package-load-with-check
 erc
 (
  ;; spell check
  ;;TODO
;;  (add-hook 'erc-mode-hook
;;          '(lambda ()
;;             (flyspell-mode-on)))
  )
 )
